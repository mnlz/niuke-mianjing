# 简历编辑区展示优化

## 目标

合并“上传时识别结果”和“完整解析文本”，减少重复内容与纵向空间占用，同时保留完整文本编辑和确认流程。

## 设计

- 保留姓名、邮箱、手机号、页数和字符数元数据。
- 将识别出的分区显示为正文上方的紧凑标签，不再渲染分区摘要卡片。
- 标签与完整文本共同位于“简历内容”编辑区内，正文继续使用可编辑文本域。
- 保留“待确认 / 已确认”、修改后撤销确认、确认使用简历等现有行为。
- 窄屏时标签自然换行，不增加新的交互和依赖。

## 验证

- 源码回归测试断言新文案与结构存在，旧的重复展示文案消失。
- 运行现有前端测试和生产构建。
